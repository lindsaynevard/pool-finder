rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Schedule data is public (read-only) — no login required
    match /schedules/{document=**} {
      allow read: if true;
      allow write: if false;
    }
  }
}
